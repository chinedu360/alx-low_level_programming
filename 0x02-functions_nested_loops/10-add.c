#include "main.h"
/**
 * add - the funtion adds two ints and returns the val
 * @one: the first num
 * @two: the second num
 * Return: one + two
 */
int add(int one, int two)
{
	return (one + two);
}
