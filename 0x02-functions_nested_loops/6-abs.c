#include "main.h"
/** 
 * _abs - a function thart comutes the absolute valueof an integer
 * @ab: interger input
 * Return: absolute value of ab
 */
int _abs(int ab)
{
	return (ab * ((ab > 0) - (ab < 10)));
}
